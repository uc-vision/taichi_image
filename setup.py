from setuptools import find_packages, setup

setup(name='taichi_image',
      version='0.1',
      install_requires=[
          'taichi'
      ],
      packages=find_packages(),
      entry_points={}
)
